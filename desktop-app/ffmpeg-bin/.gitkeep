# This folder should contain FFmpeg binaries for bundling
# 
# For Windows: ffmpeg.exe, ffprobe.exe
# For macOS: ffmpeg, ffprobe
# For Linux: ffmpeg, ffprobe
#
# Download FFmpeg from: https://ffmpeg.org/download.html
# Or use the automated GitHub Actions workflow which downloads them automatically.
#
# The binaries are NOT committed to git (see .gitignore).
# They are downloaded during CI/CD build process.
